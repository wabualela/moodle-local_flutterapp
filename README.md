# A Moodle local plugin that had a custom web service to the flutter mobile app
currently web services is:
## get user certificates
### A funciton that get a course id and return the auth user certificates.
